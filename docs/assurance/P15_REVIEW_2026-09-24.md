# P15 contracts source review — 2026-09-24

## Source and publication

- Reviewed clean `matched-fill-settlement-20260831` at `90effe66df32f12b2d770d13a5edc8d77fd5a7f9` against fetched `origin/main` `dc1d7c2dc16b08dae4012b4885030c24effc5d89`: ahead 23, behind 0. No remote branch at that name. GitHub returned no open PR or branch workflow runs.
- Diff: 63 files, 7,547 additions and 618 deletions. Main changes: signed matched and pair settlement, quote-denominated funding, signed event orders, UMA proposer-funded bonds, and PauseGuardian ring migration. Local deployment and assurance material also changed.
- Review found no new source defect requiring a protocol edit. This is a scoped review, not an independent security audit or deployed storage proof. Draft PR preparation is reasonable. Merge, upgrade, or activation requires exact-head CI, consumer parity, fork/deployed checks, and named security/governance acceptance.

## Review and checks

- MatchedFillRouter validates wallet/1271 signatures, executor, domain, deadline, nonce, roles, limit price, order quantity, and pair shape before settlement. Replay markers precede engine calls; reverts unwind all legs. Partial maker fills allocate collateral and max fee by cumulative signed totals. The engine rechecks position state, margin, KYC, caps, and mark divergence. Checked affected interfaces and delegatecall-emitted event signatures against implementations and tests.
- Existing storage member prefixes were checked against `origin/main` with Solidity 0.8.24/Cancun: 340 member checks pass; only the trailing 128-to-721 PauseGuardian ring expansion is allowed. This does not inspect deployed proxy slots or prove a downgrade path. A wrapped legacy ring is rotated before its first new write.
- Current focused reruns: `forge test --threads 1 --offline --match-contract 'CanonicalVectorsTest|MatchedFillRouterTest' --skip 'script/**'` passed 44/44; `forge test --threads 1 --offline --match-path test/PerpEngine.t.sol --skip 'script/**'` passed 254/254; `python3 script/check-storage-layout.py` passed 340 checks; `forge fmt --check` and `git diff --check origin/main...HEAD` passed.
- P05's recorded bounded full run passed 1,567/0 with one explicit Base Sepolia fork skip, plus build/size and gas checks. Full suite was not rerun for P15. Targeted secret-pattern review found only documented public Anvil keys and env variable names in touched local scripts; no new credential value was found.

## Required release gates

- Exact-head PR and CI are absent. CI on `origin/main` does not cover this branch. The remote branch, PR, and CI must be created and checked by the publishing coordinator.
- ABI declarations add quote funding, matched fill, pair, signed event, and resolution events. Legacy unsigned event selectors still exist but revert; legacy funding pushes disable after quote activation. Engine, API, indexer, signer, and operations consumers must match these behaviors and the new EIP-712 domains before upgrade.
- Base Sepolia fork ceremony was explicitly skipped without RPC. Validate the actual proxy implementation, storage/config, router/operator registration, signer, event path, and rollback on an authorized fork/preview. The source layout check is not chain evidence.
- PerpEngine runtime size is recorded as 24,300 bytes, 276 bytes below EIP-170. Slither and independent security review remain open. Finance/legal/product and governance must approve activation and funding migration/downgrade policy.
- `vibe status` shows shipped People Markets sessions and pending submissions, but does not tie this commit to a shipped session. Exact-SHA shipment remains unverified. Do not flush unrelated submissions.

No push, PR, deployment, broadcast, external service mutation, or protocol code edit was performed in this review.
