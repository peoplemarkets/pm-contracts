# Contracts assurance handoff — 2026-09-23

## Source and authority

Caveman full and Ponytail full were read and enabled before work.

The assigned checkout is `matched-fill-settlement-20260831`, starting at `d930bf870a63d678fd97b3a9a91b898f3026335e`. It started clean. The stored `origin/main` is `dc1d7c2dc16b08dae4012b4885030c24effc5d89`; 21 commits are local to the feature branch relative to that ref. Sandbox fetch and GitHub queries initially failed DNS. An authorized read-only network retry succeeded: origin/main remains unchanged and the feature branch is ahead 21, behind 0. Latest five remote runs are successful on origin/main, including run `35700779578`; these do not cover this local branch. Branch-specific PR and workflow queries both returned empty lists. Open issues are #14 (event-surplus vesting/indexer fifth bucket) and #1 (admin proposal boundaries). No source reconciliation was necessary.

Read P01's finance handoff and proposed integer vectors from `pm-context-finance-risk-20260923` as read-only context. During this lane, P00 relayed owner approval for proposed 1x, per-account event shadow inventory and zero event fees as local feature-off P02/P12 test fixtures only. D-0023 remains proposed; finance, legal and activation acceptance remain pending. Other proposed caps/funding/product choices are not silently accepted. The Solidity vectors describe live-contract source math; P02/P12 may reuse the LMSR arithmetic within that limited test-fixture authorization, with no on-chain calls. No paper runtime or chain invocation was added.

## Bounded findings and changes

- The existing Base Sepolia fork ceremony returned normally when RPC was absent, which produced a misleading passing test. It now uses Foundry's explicit skip marker.
- Canonical arithmetic vectors exercise the actual Solidity LMSR, position, fee and funding libraries. Values use integer strings for consumers; money and shares use 1e6, price and quote funding indexes use 1e18. Signed division truncates toward zero. In LMSR rows, q1 is the selected outcome and q2 is the other outcome; swap them for the opposite side. Buy inputs are gross LMSR cost, before any separately approved product fee. Selling exactly the minted quantity from the post-buy state returns the listed sellReturn6: balanced/favorite rows lose one subunit; underdog/asymmetric rows return the full input. These describe source behavior, not owner-approved paper rules or active-chain parameters.
- An inventory comparison found 33 assigned versus 36 actual test files. The external full wrapper at `pm-infra/deploy/local/scripts/contracts-check.sh` has a stale `EXPECTED_TESTS` list. It omits pre-existing `test/MatchedFillRouter.t.sol` and `test/events/EventMarketRouterUpgrade.t.sol`, and the added `test/CanonicalVectors.t.sol`. P09 owns wrapper repair. P05 does not edit infra. The README/wrapper's fork-no-op wording must also become explicit skipped-proof wording.

## Security review map

- **Storage:** Source comparison against stored origin/main preserves all 23 existing struct prefixes across `StorageLib.sol`, `UMAAdapter.sol` and `EventMarketRouter.sol`. A separate Solidity 0.8.24/Cancun storage-layout comparison passed 340 recursive member slot/offset/type checks across all pre-existing layout-bearing contracts, namespaced core layouts, and EventMarket. The only permitted size change is the trailing 128-to-721 ring array. All four dependency submodules match their tracked SHAs and are unchanged from origin/main. This does not compare deployed chain storage. Quote funding has a separate `people.markets.funding.quote.v1` namespace; legacy funding is not reinterpreted. Router replay and UMA bond/verdict mappings append to their layouts. `EventMarket` appends `_resolutionAssertionId`; existing minimal clones remain bound to their original implementation.
- **Legacy ring:** The trailing `Observation[RING_SIZE]` array expands 128 to 721, preserving head/length and the first 128 entries. Wrapped rings rotate before expansion. Existing tests cover ordering, full 3600-second coverage at five-second cadence, and freezing windows beyond observable history. Symbolic struct comparison alone cannot prove this migration.
- **Authority:** Router implementations disable initializers; UUPS upgrades require governance. Event execution requires operator, signed executor, wallet EIP-712/1271 authorization, factory membership, nonce and deadline. Legacy unsigned event selectors remain but revert. Matched person/pair fills reserve replay state before engine calls; any failed leg reverts the entire transaction.
- **Spenders:** Event traders approve EventMarketRouter; it resets the transient market allowance. UMA proposals pull the configured bond from the external proposer through the market and adapter; the proposer remains the economic asserter. Settlement payout collateral is not the bond source. Perp settlement retains vault collateral flow. Consumers must use exported spender/token bindings, not infer a universal spender.
- **ABI:** Source comparison preserved all 97 existing event declarations in 10 touched interface/implementation files and found 10 additions. New quote funding, matched-fill/pair, signed-event and resolution events require matching consumers. Existing legacy selectors do not guarantee equivalent behavior: unsigned event calls now revert and legacy funding pushes are disabled after quote activation. Coordinate API/engine/indexer before an upgrade.
- **Funding migration:** Old positions default to zero quote-index entry; pre-activation funding is waived intentionally. Downgrade policy and active-chain migration acceptance remain governance/security decisions.

## Reproducible checks

- `python3 script/check-storage-layout.py` compares Solidity 0.8.24/Cancun compiler layouts against current `origin/main`; set `SOLC` when the compiler is installed elsewhere. Saved output: `storage-layout-result.json`. It checks source layout, not deployed proxy implementation or raw chain slots.
- `forge test --threads 1 --offline --match-contract CanonicalVectorsTest` verifies every exported expected arithmetic result. The four LMSR rows were initially emitted by the actual Solidity library, not calculated using floating point.
- `forge fmt --check` and `git diff --check` check formatting.

## Verification status

- **Aggregate:** 12/12 partitions cover all 36 discovered test files: **1,567 passed, zero failed, one explicit fork skip**, in 2,263.33 seconds. Native Solidity 0.8.24/Cancun, optimizer 200, via-IR, 13 fuzz tests at 10,000 runs, and 15 invariant properties across three suites at 1,000 runs/depth 100 (300,000 handler calls). Foundry 1.8.1 reports each invariant suite as one aggregate test. Perp invariants allowed 16 handler reverts under the unchanged native `fail_on_revert=false` setting; no invariant failed.
- **Focused:** Initial math checks passed 146/0/0, overlapping the aggregate. Canonical fixture tests include 24 expected arithmetic outputs. Storage checks passed 340/0. Existing local deployment/config guards and event metric/resolution/recovery simulations passed within the aggregate; these are local simulations, not provider proof.
- **Resource boundary:** The initial all-at-once `prlimit --as=3221225472 -- forge test --threads 1 --offline --no-match-contract UpgradeCeremonyForkTest` failed compilation with `std::bad_alloc` after 586.87 seconds, before executing tests. The successful partition driver preserves the native profile, uses at most three test files and one worker, and caps each process address space at 3 GiB. This is not an aggregate Docker memory cap. The durable runner is `python3 script/check-bounded-tests.py`; coordinate the shared heavy slot first. The executed temporary runner used the same partition loop; final script adds profile and file-coverage assertions, separately verified. No green partition was rerun.
- **Build/size:** `prlimit --as=3221225472 -- forge build --sizes --threads 1 --offline` passed; remaining compilation took 328.33 seconds. Runtime bytes: PerpEngine 24,300 (**276 bytes headroom** under EIP-170), LPVault 22,825, MatchedFillRouter 16,983, EventMarket 13,540, EventMarketRouter 8,122, PauseGuardian 8,823, UMAAdapter 9,209. PerpEngine has little room for further growth. Existing compiler/lint warnings remain; successful build is not a warning-free claim.
- **Gas:** A cached, two-test `forge test --threads 1 --offline --gas-report --match-contract PauseGuardianTest --match-test 'test_Observe_(MigratesWrappedLegacyRingWithoutLosingOrder|RetainsFullFrozenWindowAtMinimumCadence)'` run (same 3 GiB cap, other tests/scripts skipped) passed 2/0/0 in 7.49 seconds. Wrapped migration `PauseGuardian.observe` measured 983,222 gas for one call. Legacy harness observation measured min 69,513, average 1,163,663, median 1,166,805, max 2,264,743 across 721 calls. These are local test-state measurements, not live gas guarantees. The 906,204,090 whole-test gas figure contains a loop; it is not one observation transaction.
- **Light checks:** `forge fmt --check`, `git diff --check`, Python AST syntax, and touched-file secret-pattern review passed. Machine-readable counts and partition inputs are in `gate-results-2026-09-23.json`.
- **Unrun:** External infra wrapper was not invoked: inventory preflight mismatches and its documented service-stop prerequisite lacks authorization/ownership evidence. No shared service stopped or changed. Deep profile (50,000 fuzz, 5,000 invariants at depth 200) was outside this bounded gate. Slither is unavailable. External RPC fork explicitly skipped because no RPC was configured. Deployed storage/config parity, independent security review, finance/legal/activation and owner release acceptance remain open.

## Integration and release boundary

P09/P14 repair the wrapper inventory and integrate the vectors. P01/product/finance/legal owners accept or revise proposed paper choices before enablement. P04/P06/P02 prove exact consumer parity. Source and local results cannot establish CI, preview, deployed/live or owner acceptance. Escalated `vibe status` shows two earlier people-markets sessions marked shipped, but no evidence ties this lane's subsequent commits to either session; shipped inclusion remains unverified. No push, PR mutation, preview, deployment, broadcast, key change or network mutation occurred.
